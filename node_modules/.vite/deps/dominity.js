// node_modules/@preact/signals-core/dist/signals-core.module.js
var i = Symbol.for("preact-signals");
function t() {
  if (!(s > 1)) {
    var i3, t2 = false;
    while (void 0 !== h) {
      var r = h;
      h = void 0;
      f++;
      while (void 0 !== r) {
        var o2 = r.o;
        r.o = void 0;
        r.f &= -3;
        if (!(8 & r.f) && c(r)) try {
          r.c();
        } catch (r2) {
          if (!t2) {
            i3 = r2;
            t2 = true;
          }
        }
        r = o2;
      }
    }
    f = 0;
    s--;
    if (t2) throw i3;
  } else s--;
}
var o = void 0;
function n(i3) {
  var t2 = o;
  o = void 0;
  try {
    return i3();
  } finally {
    o = t2;
  }
}
var h = void 0;
var s = 0;
var f = 0;
var v = 0;
function e(i3) {
  if (void 0 !== o) {
    var t2 = i3.n;
    if (void 0 === t2 || t2.t !== o) {
      t2 = { i: 0, S: i3, p: o.s, n: void 0, t: o, e: void 0, x: void 0, r: t2 };
      if (void 0 !== o.s) o.s.n = t2;
      o.s = t2;
      i3.n = t2;
      if (32 & o.f) i3.S(t2);
      return t2;
    } else if (-1 === t2.i) {
      t2.i = 0;
      if (void 0 !== t2.n) {
        t2.n.p = t2.p;
        if (void 0 !== t2.p) t2.p.n = t2.n;
        t2.p = o.s;
        t2.n = void 0;
        o.s.n = t2;
        o.s = t2;
      }
      return t2;
    }
  }
}
function u(i3) {
  this.v = i3;
  this.i = 0;
  this.n = void 0;
  this.t = void 0;
}
u.prototype.brand = i;
u.prototype.h = function() {
  return true;
};
u.prototype.S = function(i3) {
  if (this.t !== i3 && void 0 === i3.e) {
    i3.x = this.t;
    if (void 0 !== this.t) this.t.e = i3;
    this.t = i3;
  }
};
u.prototype.U = function(i3) {
  if (void 0 !== this.t) {
    var t2 = i3.e, r = i3.x;
    if (void 0 !== t2) {
      t2.x = r;
      i3.e = void 0;
    }
    if (void 0 !== r) {
      r.e = t2;
      i3.x = void 0;
    }
    if (i3 === this.t) this.t = r;
  }
};
u.prototype.subscribe = function(i3) {
  var t2 = this;
  return E(function() {
    var r = t2.value, n2 = o;
    o = void 0;
    try {
      i3(r);
    } finally {
      o = n2;
    }
  });
};
u.prototype.valueOf = function() {
  return this.value;
};
u.prototype.toString = function() {
  return this.value + "";
};
u.prototype.toJSON = function() {
  return this.value;
};
u.prototype.peek = function() {
  var i3 = o;
  o = void 0;
  try {
    return this.value;
  } finally {
    o = i3;
  }
};
Object.defineProperty(u.prototype, "value", { get: function() {
  var i3 = e(this);
  if (void 0 !== i3) i3.i = this.i;
  return this.v;
}, set: function(i3) {
  if (i3 !== this.v) {
    if (f > 100) throw new Error("Cycle detected");
    this.v = i3;
    this.i++;
    v++;
    s++;
    try {
      for (var r = this.t; void 0 !== r; r = r.x) r.t.N();
    } finally {
      t();
    }
  }
} });
function d(i3) {
  return new u(i3);
}
function c(i3) {
  for (var t2 = i3.s; void 0 !== t2; t2 = t2.n) if (t2.S.i !== t2.i || !t2.S.h() || t2.S.i !== t2.i) return true;
  return false;
}
function a(i3) {
  for (var t2 = i3.s; void 0 !== t2; t2 = t2.n) {
    var r = t2.S.n;
    if (void 0 !== r) t2.r = r;
    t2.S.n = t2;
    t2.i = -1;
    if (void 0 === t2.n) {
      i3.s = t2;
      break;
    }
  }
}
function l(i3) {
  var t2 = i3.s, r = void 0;
  while (void 0 !== t2) {
    var o2 = t2.p;
    if (-1 === t2.i) {
      t2.S.U(t2);
      if (void 0 !== o2) o2.n = t2.n;
      if (void 0 !== t2.n) t2.n.p = o2;
    } else r = t2;
    t2.S.n = t2.r;
    if (void 0 !== t2.r) t2.r = void 0;
    t2 = o2;
  }
  i3.s = r;
}
function y(i3) {
  u.call(this, void 0);
  this.x = i3;
  this.s = void 0;
  this.g = v - 1;
  this.f = 4;
}
(y.prototype = new u()).h = function() {
  this.f &= -3;
  if (1 & this.f) return false;
  if (32 == (36 & this.f)) return true;
  this.f &= -5;
  if (this.g === v) return true;
  this.g = v;
  this.f |= 1;
  if (this.i > 0 && !c(this)) {
    this.f &= -2;
    return true;
  }
  var i3 = o;
  try {
    a(this);
    o = this;
    var t2 = this.x();
    if (16 & this.f || this.v !== t2 || 0 === this.i) {
      this.v = t2;
      this.f &= -17;
      this.i++;
    }
  } catch (i4) {
    this.v = i4;
    this.f |= 16;
    this.i++;
  }
  o = i3;
  l(this);
  this.f &= -2;
  return true;
};
y.prototype.S = function(i3) {
  if (void 0 === this.t) {
    this.f |= 36;
    for (var t2 = this.s; void 0 !== t2; t2 = t2.n) t2.S.S(t2);
  }
  u.prototype.S.call(this, i3);
};
y.prototype.U = function(i3) {
  if (void 0 !== this.t) {
    u.prototype.U.call(this, i3);
    if (void 0 === this.t) {
      this.f &= -33;
      for (var t2 = this.s; void 0 !== t2; t2 = t2.n) t2.S.U(t2);
    }
  }
};
y.prototype.N = function() {
  if (!(2 & this.f)) {
    this.f |= 6;
    for (var i3 = this.t; void 0 !== i3; i3 = i3.x) i3.t.N();
  }
};
Object.defineProperty(y.prototype, "value", { get: function() {
  if (1 & this.f) throw new Error("Cycle detected");
  var i3 = e(this);
  this.h();
  if (void 0 !== i3) i3.i = this.i;
  if (16 & this.f) throw this.v;
  return this.v;
} });
function w(i3) {
  return new y(i3);
}
function _(i3) {
  var r = i3.u;
  i3.u = void 0;
  if ("function" == typeof r) {
    s++;
    var n2 = o;
    o = void 0;
    try {
      r();
    } catch (t2) {
      i3.f &= -2;
      i3.f |= 8;
      g(i3);
      throw t2;
    } finally {
      o = n2;
      t();
    }
  }
}
function g(i3) {
  for (var t2 = i3.s; void 0 !== t2; t2 = t2.n) t2.S.U(t2);
  i3.x = void 0;
  i3.s = void 0;
  _(i3);
}
function p(i3) {
  if (o !== this) throw new Error("Out-of-order effect");
  l(this);
  o = i3;
  this.f &= -2;
  if (8 & this.f) g(this);
  t();
}
function b(i3) {
  this.x = i3;
  this.u = void 0;
  this.s = void 0;
  this.o = void 0;
  this.f = 32;
}
b.prototype.c = function() {
  var i3 = this.S();
  try {
    if (8 & this.f) return;
    if (void 0 === this.x) return;
    var t2 = this.x();
    if ("function" == typeof t2) this.u = t2;
  } finally {
    i3();
  }
};
b.prototype.S = function() {
  if (1 & this.f) throw new Error("Cycle detected");
  this.f |= 1;
  this.f &= -9;
  _(this);
  a(this);
  s++;
  var i3 = o;
  o = this;
  return p.bind(this, i3);
};
b.prototype.N = function() {
  if (!(2 & this.f)) {
    this.f |= 2;
    this.o = h;
    h = this;
  }
};
b.prototype.d = function() {
  this.f |= 8;
  if (!(1 & this.f)) g(this);
};
function E(i3) {
  var t2 = new b(i3);
  try {
    t2.c();
  } catch (i4) {
    t2.d();
    throw i4;
  }
  return t2.d.bind(t2);
}

// node_modules/dominity/src/dominity.js
var state = d;
var derived = w;
var DominityReactive = u;
function $el(qry, ...args) {
  return el(new DominityElement(qry).elem, ...args);
}
function $$el(qry) {
  let elemArr = [];
  document.querySelectorAll(qry).forEach((e2) => {
    elemArr.push(new DominityElement(e2));
  });
  return elemArr;
}
var el = function(tagname, ...args) {
  let elem = typeof tagname == "string" ? document.createElement(tagname) : tagname;
  let dElem = new DominityElement(elem);
  args.forEach((arg, index) => {
    if (typeof arg == "string") {
      let textNode = document.createTextNode(arg);
      elem.appendChild(textNode);
    } else if (typeof arg == "function" && typeof arg() == "string") {
      let textNode = document.createTextNode(arg());
      E(() => {
        textNode.data = arg();
      });
      elem.appendChild(textNode);
    } else if (arg instanceof DominityReactive) {
      let textNode = document.createTextNode(arg.value);
      elem.appendChild(textNode);
      E(() => {
        textNode.data = arg.value;
      });
    } else if (arg instanceof DominityElement) {
      elem.appendChild(arg.elem);
    } else if (typeof arg == "object" || typeof arg == "function" && typeof arg() == "object") {
      dElem.attr(arg);
    } else if (arg instanceof Array) {
      arg.forEach((ar) => {
        if (ar instanceof DominityElement) {
          elem.appendChild(ar.elem);
        } else {
          throw new Error("Dominity Error: invalid element type passed in as array ,all elements of the array should be of type DominityElement");
        }
      });
    } else {
      throw new Error(`Dominity Error: invalid type ${typeof arg} passed in as argument to dominity builder function`);
    }
  });
  return dElem;
};
var DominityElement = class _DominityElement {
  /**
   * 
   * @param {string|HTMLElement} qry 
   * @returns {this}
   */
  constructor(qry) {
    if (typeof qry == "string") {
      this.elem = document.querySelector(qry);
    } else {
      this.elem = qry;
    }
    if (this.elem == null) {
      console.error(`DominityError: element of query '${qry}'  NOT  FOUND `);
      return false;
    }
  }
  /**
   * sets inner HTML
   * @param {null |string} val - html markup as string ,returns elements innerHTML if left empty 
   * @returns {this}
   */
  html(val) {
    if (val == null) {
      return this.elem.innerHTML;
    } else {
      if (typeof val == "function") {
        E(() => {
          this.html(val());
        });
      } else {
        this.elem.innerHTML = val;
      }
      return this;
    }
  }
  /**
   * used to apply css styling to elements
   * @param {object|string} prp -pass in an object containing key value pairs of properties or a single string propertyname to be set or get
   * @param {undefined|stirng} val -if left empty the single string property computed value is returned else its set to the value passed in
   * @returns {this}
   */
  css(prp, val = void 0) {
    if (typeof prp == "string") {
      if (val == void 0) {
        return window.getComputedStyle(this.elem, null).getPropertyValue(prp);
      } else {
        this.elem.style[prp] = val;
        return this;
      }
    } else if (typeof prp == "object") {
      Object.assign(this.elem.style, prp);
      return this;
    } else if (typeof prp == "function") {
      E(() => {
        this.css(prp());
      });
      return this;
    }
  }
  addClass(...classnames) {
    this.elem.classList.add(...classnames);
    return this;
  }
  removeClass(...classnames) {
    this.elem.classList.remove(...classnames);
    return this;
  }
  removeAttr(...attrs) {
    this.elem.removeAttribute(...attrs);
    return this;
  }
  /**
   * applies a stylesheet to the element
   * @param {string} cssStringy -the css string, $this is replaced with a random class name
   * @returns {this}
   */
  $css(cssStringy, classRetriever) {
    if (cssStringy instanceof DominityReactive) {
      this.elem.classList.add(cssStringy.value);
      return this;
    }
    let randomClassName = "d-styler-" + Math.floor(Math.random() * 1e3 + Date.now());
    let parsed = cssStringy.replaceAll("$this", "." + randomClassName);
    if (!document.querySelector("#dominity-style-injected")) {
      let s3 = document.createElement("style");
      s3.id = "#dominity-style-injected";
      s3.innerHTML += parsed;
      document.body.append(s3);
    } else {
      document.querySelector("#dominity-style-injected").innerHTML += parsed;
    }
    this.elem.classList.add(randomClassName);
    if (classRetriever != void 0 && classRetriever instanceof DominityReactive) {
      classRetriever.value = randomClassName;
    }
    return this;
  }
  /**
   * used to add attributes to an element
   * @param {object|string} prp -pass in an object containing key value pairs of attributes or a single string attributename to be set or get
   * @param {undefined|stirng} val -if left empty the single string atribute value is returned else its set to the value passed in
   * @returns {this}
   */
  attr(prp, val = void 0) {
    if (typeof prp == "string") {
      if (val == void 0) {
        return this.elem.getAttribute(prp);
      } else {
        this.elem.setAttribute(prp, val);
        return this;
      }
    } else if (typeof prp == "object") {
      let attrs = Object.keys(prp);
      let vals = Object.values(prp);
      attrs.forEach((p3, i3) => {
        if (vals[i3] instanceof DominityReactive) {
          E(() => {
            this.attr(p3, vals[i3].value);
          });
        } else if (typeof vals[i3] != "function") {
          this.attr(p3, vals[i3]);
        } else if (typeof vals[i3] == "function") {
          E(() => {
            this.attr(p3, vals[i3]());
          });
        }
      });
      return this;
    }
  }
  /**
   * binds a state with a class
   * @param {DominityReactive} state  
   * @param {string} trueclass -classes to be added when state is true seperated by space
   * @param {string} falseclass -classes to be added when state is false seperated by space
   */
  bindClass(state2, trueclass, falseclass) {
    E(() => {
      if (state2.value) {
        this.elem.classList.add(...trueclass.split(" "));
        if (falseclass != void 0) {
          this.elem.classList.remove(...falseclass.split(" "));
        }
      } else {
        this.elem.classList.remove(...trueclass.split(" "));
        if (falseclass != void 0) {
          this.elem.classList.add(...falseclass.split(" "));
        }
      }
    });
    return this;
  }
  bindAttr(state2, attr, value = "") {
    E(() => {
      if (state2.value) {
        this.attr(attr, value);
      } else {
        this.removeAttr(attr);
      }
    });
    return this;
  }
  /**
   * used to attach events to an element
   * @param {string} e -event name
   * @param {Function} cb- function to be called  
   * @param {boolean} bub -event capture false by default
   * @returns {this}
   */
  on(e2, cb, bub = false) {
    this.elem.addEventListener(e2, cb, bub);
    return this;
  }
  /** removes the element from the dom */
  remove() {
    this.elem.remove();
    return this;
  }
  //css based rerenderer 
  /**
   * shows and hides the element based on truthineess of passed in state or return value of function that utilises a state
   * @param {DominityReactive|Function} expression -reactive state whos truth value affects the rendering  or function
   * @returns {this}
   */
  showIf(expression) {
    let storedDisplay = this.css("display") != "none" ? this.css("display") : "block";
    if (typeof expression == "function") {
      E(() => {
        this.showIf(expression());
      });
      return this;
    } else if (expression instanceof DominityReactive) {
      E(() => {
        this.showIf(expression.value);
      });
    } else if (expression) {
      this.elem.style.display = storedDisplay;
    } else {
      this.elem.style.display = "none";
    }
    return this;
  }
  //loop
  /**
   * iterates over a list and renders a component for each item
   * @param {DominityReactive} list-reactive state iterable who will be iterated 
   * @param {Function} callback the function must return a component or the function must be a component (this function recives (item , count,parent elements reference ))
   * @returns {this}
   */
  forEvery(list, callback) {
    let elemS = this;
    if (list instanceof DominityReactive) {
      E(() => {
        elemS.elem.innerHTML = "";
        list.value.forEach((item, count) => {
          n(() => {
            let c2 = callback(item, count, elemS);
            if (c2) {
              c2.addTo(this.elem);
            }
          });
        });
      });
      return this;
    }
    console.error("DominityError: list item for .forEvery has to be a reactive object made with state() and iterable");
    return this;
  }
  /**
   * used to run a function with reference to element 
   * @param {Function} func a function that recives js refernce and dominity reference as first and second parameters 
   * @returns {this} 
   */
  withRef(func) {
    func(this.elem, this);
    return this;
  }
  /**
   * used to store the elements javascript/DOminity refernce to a state
   * @param {DominityReactive} ref 
   * @param {boolean} org if true js reference is returned else its dominity reference
   * @returns 
   */
  giveRef(ref, org = true) {
    if (ref instanceof DominityReactive) {
      ref.value = org ? this.elem : this;
    } else {
      ref = org ? this.elem : this;
    }
    return this;
  }
  /**
   * used to model an input's value to a state (2 way databinding)
   * @param {DominityReactive} target state to be modeled to  
   * @param {{debounce?:number,throttle:?number}} options -options are used to set optional throttle and debounce values incase 
   * @returns 
   */
  model(target, options) {
    let attr = "value";
    if (this.attr("type") == "checkbox") {
      attr = "checked";
    }
    if (target instanceof DominityReactive) {
      if ((options == null ? void 0 : options.debounce) == void 0 && (options == null ? void 0 : options.throttle) == void 0) {
        E(() => {
          if (!(target.value instanceof Array)) {
            this.elem[attr] = target.value;
          } else {
            if (target.value.includes(this.elem.name)) {
              this.elem[attr] = true;
            } else {
              this.elem[attr] = false;
            }
          }
        });
      } else {
        this.elem[attr] = target.value;
      }
      let timeoutId;
      let lastcalltime = 0;
      this.on("input", () => {
        let val = this.elem.value;
        if (this.attr("type") == "number") {
          if (val == "") {
            val = "0";
          }
          val = parseFloat(val);
        }
        if (attr == "checked") {
          if (!(target.value instanceof Array)) {
            if (this.elem.checked) {
              val = true;
            } else {
              val = false;
            }
          } else {
            if (this.elem.checked) {
              val = [...target.value, this.elem.name];
            } else {
              val = target.value.filter((t2) => t2 != this.elem.name);
            }
          }
        }
        if ((options == null ? void 0 : options.debounce) == void 0 && (options == null ? void 0 : options.throttle) == void 0) {
          target.value = val;
        } else if (options == null ? void 0 : options.debounce) {
          clearTimeout(timeoutId);
          timeoutId = setTimeout(() => {
            target.value = val;
          }, options == null ? void 0 : options.debounce);
        } else if (options == null ? void 0 : options.throttle) {
          const now = (/* @__PURE__ */ new Date()).getTime();
          if (now - lastcalltime < (options == null ? void 0 : options.throttle)) {
            return;
          }
          lastcalltime = now;
          target.value = val;
        }
      });
    }
    return this;
  }
  /**
   * 
   * @param {object} props properties to be animated as an object and their final values 
   * @param {number} duration duration in milliseconds
   * @param {string} easing easing function 
   * @param {Function} callback -call back that runs after the animation ends 
   * @returns {this}
   */
  animate(props, duration, easing = "linear", callback = void 0) {
    let priorkeyframes = {};
    Object.keys(props).forEach((prop) => {
      if (props[prop] instanceof Array) {
        priorkeyframes[prop] = props[prop][0];
        props[prop] = props[prop][1];
      } else {
        priorkeyframes[prop] = this.css(prop);
      }
    });
    let animation = this.elem.animate([priorkeyframes, props], {
      duration: duration * 1e3,
      easing,
      fill: "forwards"
    });
    animation.onfinish = () => {
      this.css(props);
      if (typeof callback === "function") {
        callback(this);
      }
    };
    return this;
  }
  /**
  * appends the element to another element provided 
  * @param {DominityElement|HTMLELement} elm -parent element to add to
  * @returns {this}
  */
  addTo(elm) {
    if (elm instanceof _DominityElement) {
      elm.elem.appendChild(this.elem);
    } else {
      elm.appendChild(this.elem);
    }
    return this;
  }
  /**
   * returns nth children of the element 
   * @param {number} index 
   * @returns 
   */
  child(index) {
    return new _DominityElement(this.elem.children[index]);
  }
  /**
       * extends the current dominity element with the provided object
       * @param {object} extension -an object containing methods and properties to be added to the current dominity element
       * @returns {this}
  */
  extend(extension) {
    Object.assign(this, extension);
    return this;
  }
};
var DominityRouter = class {
  constructor() {
    this.routeMap = {};
    this.defaultPath = void 0;
  }
  /**
   * sets the routes
   * @param {object} routeMap
   * 
   * @example
   * 
   * ### using component directly
   * ```
   * {
   * "/home":{
   *    component:home()
   * }
   * ,"about":{...}
   * 
   * }
   * ```
   * 
   * ### using component as function 
   * ```
   * 
   * {
   *  "/home":{
   *     getComponent:home,
   *   onLoad(){
   *      console.log('home')
   *  },
   *  layout(component){
   *    return someLayout(component)
   *  }
   * }
   * 
   * }
   * ```
   *  
   */
  setRoutes(routeMap = {}) {
    Object.keys(routeMap).forEach((key) => {
      let routeobj = {};
      let routeData = routeMap[key];
      routeobj.viewKey = state(routeData.isDefault != null && routeData.isDefault);
      if (routeData.isDefault != null && routeData.isDefault) {
        this.defaultPath = key;
      }
      if (routeMap[key].component instanceof DominityElement) {
        routeMap[key].component.showIf(routeobj.viewKey);
      }
      if (routeMap[key].getComponent != void 0 && typeof routeMap[key].getComponent == "function") {
        E(async () => {
          if (routeobj.viewKey.value) {
            let component = await n(async () => await routeMap[key].getComponent(this));
            if (routeMap[key].layout != void 0) {
              component = routeMap[key].layout(component);
            }
            this.root.appendChild(component.withRef((r) => {
              E(() => {
                routeMap[key].componentLoaded = r;
                if (routeMap[key].onLoad != void 0) {
                  n(() => routeMap[key].onLoad(r));
                }
                if (this.onLoad != void 0) {
                  n(this.onLoad);
                }
              });
            }).elem);
          } else {
            if (routeMap[key].componentLoaded) {
              routeMap[key].componentLoaded.remove();
            }
          }
        });
      }
      this.routeMap[key] = routeobj;
    });
  }
  /**
   * sets root of router , ie the element wheer router elements will be rendered 
   * @param {HTMLElement|DominityElement} root -element to be passed in as root of router
   */
  start(root) {
    if (root instanceof DominityElement) {
      this.root = root.elem;
    } else {
      this.root = root;
    }
    addEventListener("popstate", () => {
      setTimeout(() => {
        this.assignRoute();
      }, 100);
    });
    addEventListener("load", async () => {
      setTimeout(() => {
        if (window.location.pathname == "/" || window.location.pathname == this.defaultAltPath) {
          this.routeTo(this.defaultPath);
        } else {
          this.assignRoute();
        }
      }, 200);
    });
    this.assignRoute();
  }
  async assignRoute() {
    Object.keys(this.routeMap).forEach((route) => {
      let routeData = this.routeMap[route];
      if (window.location.pathname == route) {
        routeData.viewKey.value = true;
      } else {
        routeData.viewKey.value = false;
      }
    });
  }
  /**
   * routes to a specific new route 
   * @param {string} route -
   */
  routeTo(route) {
    history.pushState(null, "", route);
    this.assignRoute();
  }
  /**
   * replaces the current route with new route
   * @param {string} route 
   */
  replaceRoute(route) {
    history.replaceState(null, "", route);
    this.assignRoute();
  }
  /**
   * reruns the route handler of current route
   */
  revalidateRoute() {
    history.go(0);
    this.assignRoute();
  }
  /**
   * a replacement for anchor tags that works with the router 
   * @param {{href:string,replace:boolean,...attrs:any}} param0 - replace if true route is replaced instead of new redirect
   * @param  {...any} args -remaining components
   * @returns 
   */
  Link({ href, replace, ...attrs }, ...args) {
    return el("a", { ...attrs }, ...args).on("click", (e2) => {
      e2.preventDefault();
      if (replace != null) {
        this.replaceRoute(e2.target.getAttribute("href"));
      } else {
        this.routeTo(href);
      }
    });
  }
  /**gets all search queries as object */
  get queries() {
    return Object.fromEntries(new URLSearchParams(window.location.search).entries());
  }
};
function lazy(path) {
  return function(router) {
    return import(path).then((s3) => s3.default(router));
  };
}
var DominityStore = class {
  constructor(name, storeData) {
    this._values = {};
    Object.keys(storeData.states).forEach((key) => {
      this._values[key] = state(storeData.states[key]);
    });
    Object.keys(storeData.actions).forEach((key) => {
      this[key] = () => {
        storeData.actions[key](this._values, this);
      };
    });
    Object.keys(storeData.getters).forEach((key) => {
      this[key] = derived(storeData.getters[key]);
    });
  }
  getRef(ref) {
    return derived(() => this._values[ref].value);
  }
  getEditableRef(ref) {
    let refer = derived(() => this._values[ref].value);
    E(() => {
      this._values[ref].value = refer.value;
    });
    return refer;
  }
  getRefs(obj) {
    if (obj && obj.edit) return this._values;
    let refobj = {};
    for (const key in this._values) {
      refobj[key] = derived(() => this._values[key].value);
    }
    return refobj;
  }
};
function createStore(name, storeData) {
  return new DominityStore(name, storeData);
}
var htmlTags = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "header",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "param",
  "picture",
  "pre",
  "progress",
  "q",
  "rb",
  "rp",
  "rt",
  "rtc",
  "s",
  "samp",
  "script",
  "section",
  "select",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "table",
  "tbody",
  "td",
  "template",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "u",
  "ul",
  "var",
  "video",
  "wbr",
  "slot"
];
var D = htmlTags.reduce((dobj, tag) => {
  dobj[tag] = (...args) => {
    let Delem = el(tag, ...args);
    dobj.root.appendChild(Delem.elem);
    return Delem;
  };
  return dobj;
}, {
  root: document.body
});
var {
  a: a2,
  abbr,
  address,
  area,
  article,
  aside,
  audio,
  b: b2,
  base,
  bdi,
  bdo,
  blockquote,
  body,
  br,
  button,
  canvas,
  caption,
  cite,
  code,
  col,
  colgroup,
  data,
  datalist,
  dd,
  del,
  details,
  dfn,
  dialog,
  div,
  dl,
  dt,
  em,
  embed,
  fieldset,
  figcaption,
  figure,
  footer,
  form,
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  header,
  hr,
  html,
  i: i2,
  iframe,
  img,
  input,
  ins,
  kbd,
  label,
  legend,
  li,
  link,
  main,
  map,
  mark,
  meta,
  meter,
  nav,
  noscript,
  object,
  ol,
  optgroup,
  option,
  output,
  p: p2,
  param,
  picture,
  pre,
  progress,
  q,
  rb,
  rp,
  rt,
  rtc,
  s: s2,
  samp,
  script,
  section,
  select,
  small,
  source,
  span,
  strong,
  style,
  sub,
  summary,
  sup,
  table,
  tbody,
  td,
  template,
  textarea,
  tfoot,
  th,
  thead,
  time,
  title,
  tr,
  u: u2,
  ul,
  video,
  wbr,
  slot
} = D;
var dominity_default = { ...D, effect: E, createStore, derived, state, DominityReactive, $el, $$el, el, DominityRouter, DominityElement };
export {
  $$el,
  $el,
  DominityElement,
  DominityReactive,
  DominityRouter,
  DominityStore,
  a2 as a,
  abbr,
  address,
  area,
  article,
  aside,
  audio,
  b2 as b,
  base,
  bdi,
  bdo,
  blockquote,
  body,
  br,
  button,
  canvas,
  caption,
  cite,
  code,
  col,
  colgroup,
  createStore,
  data,
  datalist,
  dd,
  dominity_default as default,
  del,
  derived,
  details,
  dfn,
  dialog,
  div,
  dl,
  dt,
  E as effect,
  el,
  em,
  embed,
  fieldset,
  figcaption,
  figure,
  footer,
  form,
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  header,
  hr,
  html,
  i2 as i,
  iframe,
  img,
  input,
  ins,
  kbd,
  label,
  lazy,
  legend,
  li,
  link,
  main,
  map,
  mark,
  meta,
  meter,
  nav,
  noscript,
  object,
  ol,
  optgroup,
  option,
  output,
  p2 as p,
  param,
  picture,
  pre,
  progress,
  q,
  rb,
  rp,
  rt,
  rtc,
  s2 as s,
  samp,
  script,
  section,
  select,
  slot,
  small,
  source,
  span,
  state,
  strong,
  style,
  sub,
  summary,
  sup,
  table,
  tbody,
  td,
  template,
  textarea,
  tfoot,
  th,
  thead,
  time,
  title,
  tr,
  u2 as u,
  ul,
  video,
  wbr
};
//# sourceMappingURL=dominity.js.map
